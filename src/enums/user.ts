export enum USER_ROLES {
  ADMIN = "admin",
  SUPER_ADMIN = "superAdmin",
  USER = "user",
  DRIVER = "driver",
}

export enum GENDER {
  MALE = "male",
  FEMALE = "female",
}

export enum STATUS {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum DRIVER_STATUS {
  NONE = "none",
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}
